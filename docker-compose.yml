version: '3.8'
services:
  app:
    build:
      context: ./realtime-chat-app
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ./realtime-chat-app/.env.local
    restart: unless-stopped
  